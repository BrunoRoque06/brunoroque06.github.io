{
  "scripts": {
    "audit": "lhci autorun",
    "build": "npm run clean && eleventy",
    "clean": "rm -rf _site dist",
    "deploy": "npm run build && gh-pages --dist _site --no-history",
    "start": "npm run clean && eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "2.0.0",
    "@lhci/cli": "0.10.0",
    "gh-pages": "5.0.0",
    "prettier": "2.8.4",
    "puppeteer": "19.7.0",
    "sass": "1.58.0",
    "sharp": "0.31.3"
  }
}
